package engine.maps{

	import flash.display.MovieClip;
	import engine.Map;

	public class StartScreen extends Map {
		//load game vars into 'global' memory
		
		public function StartScreen(ldr:*=null):void {
			trace('loaded');
			super(ldr);
			//
		}
	}//end class
}//end package